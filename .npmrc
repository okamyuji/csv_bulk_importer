; Inherit the frontend-scoped supply-chain policy at the repo root as well so
; any command (pnpm install, pnpm add) run from either dir honors the 1-week waiting period.
minimum-release-age=10080
frozen-lockfile=true
ignore-scripts=true
