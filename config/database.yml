<%
require Rails.root.join("lib/cpu_concurrency").to_s
%>default: &default
  adapter: mysql2
  encoding: utf8mb4
  collation: utf8mb4_0900_ai_ci
  # CPU 数 (cgroup quota 優先) から Solid Queue / Puma 双方を満たす pool を自動算出。
  # ENV DB_POOL / RAILS_MAX_THREADS で上書き可能。
  pool: <%= CpuConcurrency.db_pool %>
  username: <%= ENV.fetch("DB_USERNAME") { "root" } %>
  password: <%= ENV.fetch("DB_PASSWORD") { "password" } %>
  host:     <%= ENV.fetch("DB_HOST") { "127.0.0.1" } %>
  port:     <%= ENV.fetch("DB_PORT") { 3306 } %>
  variables:
    sql_mode: "STRICT_ALL_TABLES"

development:
  primary:
    <<: *default
    database: bulk_file_importer_development
  cache:
    <<: *default
    database: bulk_file_importer_development_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    database: bulk_file_importer_development_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    database: bulk_file_importer_development_cable
    migrations_paths: db/cable_migrate

test:
  primary:
    <<: *default
    database: bulk_file_importer_test
  cache:
    <<: *default
    database: bulk_file_importer_test_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    database: bulk_file_importer_test_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    database: bulk_file_importer_test_cable
    migrations_paths: db/cable_migrate

production:
  primary: &primary_production
    <<: *default
    database: <%= ENV.fetch("DATABASE_NAME", "bulk_file_importer") %>
    username: <%= ENV.fetch("DATABASE_USERNAME") { ENV.fetch("DB_USERNAME", "admin") } %>
    password: <%= ENV.fetch("DATABASE_PASSWORD") { ENV.fetch("DB_PASSWORD", "") } %>
    host: <%= ENV.fetch("DATABASE_HOST") { ENV.fetch("DB_HOST", "127.0.0.1") } %>
    port: <%= ENV.fetch("DATABASE_PORT") { ENV.fetch("DB_PORT", 3306) } %>
  cache:
    <<: *primary_production
    database: <%= ENV.fetch("DATABASE_NAME", "bulk_file_importer") %>_cache
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    database: <%= ENV.fetch("DATABASE_NAME", "bulk_file_importer") %>_queue
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    database: <%= ENV.fetch("DATABASE_NAME", "bulk_file_importer") %>_cable
    migrations_paths: db/cable_migrate
